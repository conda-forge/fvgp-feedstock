{% set name = "fvgp" %}
{% set version = "3.3.8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/lbl-camera/fvGP/archive/{{ version }}.tar.gz
  sha256: 044083c806fe11348c8248797a654fa526ccfa7a3e5e60325d6e35728cf325d6
  patches:
    - dep.patch

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.7
    - pip
  run:
    - python >=3.7
    - pytorch >=1.9.0
    - scipy
    - numpy
    - matplotlib-base
    - dask-core >=2021.6.2
    - distributed >=2021.6.2
    - hgdl =2.0.7
    - notebook
    - plotly
    - loguru

test:
  imports:
    - fvgp
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/lbl-camera/fvGP
  summary: Python package for highly flexible function-valued Gaussian processes (fvGP)
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jan-janssen
